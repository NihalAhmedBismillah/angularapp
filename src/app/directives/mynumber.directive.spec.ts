import { MynumberDirective } from './mynumber.directive';

describe('MynumberDirective', () => {
  it('should create an instance', () => {
    const directive = new MynumberDirective();
    expect(directive).toBeTruthy();
  });
});
